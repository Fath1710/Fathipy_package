from setuptools import setup, find_packages

setup(
    name='First_Package',
    version='1.0',
    packages=find_packages(),
    install_requires=[],
    description='Un ejemplo de la estructura de paquete de Python',
    author='Author Name',
    author_email='author@example.com'
)
